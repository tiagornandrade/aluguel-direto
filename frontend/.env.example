# Backend API base URL (no trailing slash)
NEXT_PUBLIC_API_URL="http://localhost:4000"

# NextAuth - NEXTAUTH_SECRET is required for login (including dev "Entrar como..."). Generate: openssl rand -base64 32
NEXTAUTH_SECRET=
NEXTAUTH_URL="http://localhost:3000"

# Internal API key (must match backend .env). Used by proxy to call backend with X-Api-Key.
INTERNAL_API_KEY=

# Dev mode: access SaaS without real login, switch Proprietário/Inquilino. Set to "true" or "1" in .env.local.
# next.config propagates DEV_MODE to NEXT_PUBLIC_DEV_MODE for the client. In dev, is also enabled when
# NODE_ENV=development and not on Vercel.
# DEV_MODE=
# NEXT_PUBLIC_DEV_MODE=
