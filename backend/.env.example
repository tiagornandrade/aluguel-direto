# Database (Neon PostgreSQL). Use one of the options below.
# Option A: single URL per environment (recommended in production: set only DATABASE_URL on the host)
# DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
# Option B: dev and prod URLs (set both in .env; app and Prisma CLI pick by NODE_ENV)
DATABASE_URL_DEV="postgresql://user:pass@host-dev/db?sslmode=require"
DATABASE_URL_PROD="postgresql://user:pass@host-prod/db?sslmode=require"

# Server
PORT=4000

# CORS (comma-separated origins; * for dev only)
CORS_ORIGINS="http://localhost:3000"

# Internal API key (must match frontend .env.local). Used by BFF/proxy for X-Api-Key.
# Generate with: openssl rand -base64 32
INTERNAL_API_KEY=
