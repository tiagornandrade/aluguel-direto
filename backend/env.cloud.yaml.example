# Copie para env.cloud.yaml e preencha os valores. Não commitar env.cloud.yaml.
# Variáveis de ambiente do backend no Cloud Run (formato aceito por gcloud run deploy --env-vars-file).

DATABASE_URL_PROD: "postgresql://user:pass@host-prod/db?sslmode=require"
CORS_ORIGINS: "https://aluguel-direto-XXX.run.app"
INTERNAL_API_KEY: ""

# Opcional: análise de documentos com IA
# OPENAI_API_KEY: "sk-..."
